{
  "name": "sales-ops-ledger",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Live data-fetch layer for the Sales Ops Ledger dashboard, pulling from the GoHighLevel API.",
  "scripts": {
    "fetch-data": "node scripts/fetch-data.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
